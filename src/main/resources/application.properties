server.port=${SERVER_PORT}
server.servlet.context-path=/${SERVICE_NAME}

spring.main.allow-bean-definition-overriding=true
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
logging.level.org.hibernate.SQL=OFF
logging.level.org.hibernate.orm.jdbc.bind=OFF

flyway.ignoreMissingMigrations=true


app.frontend.base-url=${FRONTEND_BASE_URL:http://localhost:4200/church-lite}
resend.api-key=${RESEND_KEY:}
resend.from=${RESEND_FROM:Church Lite <no-reply@smartverse.com.br>}


spring.rabbitmq.host=${RABBITMQ_HOST:localhost}
spring.rabbitmq.port=${RABBITMQ_PORT:5672}
spring.rabbitmq.password=${RABBITMQ_PASSWORD:libertpass553322}
spring.rabbitmq.username=${RABBITMQ_USERNAME:libertuser}
spring.rabbitmq.listener.simple.auto-startup=true

smart-payment.base-url=${PAYMENT_SERVICE_BASE_URL:https://app.smartverse.com.br/api/payment-service}
smart-payment.confirmed-queue=${PAYMENT_CONFIRMED_QUEUE:smart.payment.confirmed.church-lite}
smart-report.base-url=${SMART_REPORT_BASE_URL:https://app.smartverse.com.br/api/smartreport}
